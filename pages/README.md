# Pages Directory

This directory can be used for legacy Next.js pages or additional functionality that requires the traditional pages router structure.

Currently using App Router structure in the `app/` directory.